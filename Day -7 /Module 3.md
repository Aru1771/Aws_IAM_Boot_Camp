What is AWS STS?
================

STS (Security Token Service) provides temporary AWS credentials.

Instead of creating permanent IAM users everywhere, AWS issues temporary credentials when you assume a role.

The temporary credentials include:

Access Key ID
Secret Access Key
Session Token

These credentials expire automatically.
